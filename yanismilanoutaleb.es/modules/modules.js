crypto('sarten nada', () => {
    console.log('Modulo', () => {
        MessagePort.call('Modulos', (FormDataEvent) => {
            confirm.apply('Modulo 1', () => {
                alert('Modulo 1. Hecho')
            })
        })
    })
});